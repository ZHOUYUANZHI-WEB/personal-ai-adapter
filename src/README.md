# Reference implementation

The reference implementation will be added after the v0.1 schemas are validated.

The first executable workflow will remain intentionally small:

```text
Capture → Inbox → Process → Project or Knowledge
```

No specific model, task manager, editor, or vector database will be required by the core.

